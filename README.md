# reversedMessage-API
It's an endpoint that accepts a **`POST`** request. 
This endpoint will accept a JSON object with a single field **`message`** and return a JSON object with a field **`reversedMessage`** which contains the reversed version of the input message.    
